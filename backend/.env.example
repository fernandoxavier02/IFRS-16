# =============================================================================
# IFRS 16 - Backend Environment Variables Template
# =============================================================================
# IMPORTANTE: 
# 1. Copie este arquivo para .env e preencha com valores reais
# 2. NUNCA commite o arquivo .env no git
# 3. Em produção, configure estas variáveis no Cloud Run
# =============================================================================

# -----------------------------------------------------------------------------
# DATABASE
# -----------------------------------------------------------------------------
# Desenvolvimento: SQLite ou PostgreSQL local
# Produção: PostgreSQL no Render ou Cloud SQL
DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5432/ifrs16_licenses

# Cloud SQL (apenas produção)
CLOUD_SQL_USER=
CLOUD_SQL_PASSWORD=
DATABASE_URL_PROD=

# -----------------------------------------------------------------------------
# JWT AUTHENTICATION
# -----------------------------------------------------------------------------
# CRÍTICO: Gere uma chave forte e única para produção
# Exemplo: python -c "import secrets; print(secrets.token_urlsafe(64))"
JWT_SECRET_KEY=CHANGE_THIS_TO_A_STRONG_SECRET_KEY_IN_PRODUCTION
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=1440
REFRESH_TOKEN_EXPIRE_DAYS=30

# -----------------------------------------------------------------------------
# ADMIN TOKEN (LEGACY - SERÁ DEPRECIADO)
# -----------------------------------------------------------------------------
# CRÍTICO: Mude este token em produção
ADMIN_TOKEN=CHANGE_THIS_ADMIN_TOKEN_IN_PRODUCTION

# -----------------------------------------------------------------------------
# STRIPE PAYMENT INTEGRATION
# -----------------------------------------------------------------------------
# Obtenha suas chaves em: https://dashboard.stripe.com/apikeys
STRIPE_SECRET_KEY=sk_test_your_test_key_here
STRIPE_PUBLISHABLE_KEY=pk_test_your_test_key_here

# Webhook Secret (obtenha em: https://dashboard.stripe.com/webhooks)
STRIPE_WEBHOOK_SECRET=whsec_your_webhook_secret_here

# Pricing Table ID (obtenha em: https://dashboard.stripe.com/pricing-tables)
STRIPE_PRICING_TABLE_ID=prctbl_your_pricing_table_id

# Price IDs para cada plano (obtenha em: https://dashboard.stripe.com/prices)
# Plano Básico (até 3 contratos)
STRIPE_PRICE_BASIC_MONTHLY=price_your_basic_monthly_id
STRIPE_PRICE_BASIC_YEARLY=price_your_basic_yearly_id

# Plano Pro (até 20 contratos)
STRIPE_PRICE_PRO_MONTHLY=price_your_pro_monthly_id
STRIPE_PRICE_PRO_YEARLY=price_your_pro_yearly_id

# Plano Enterprise (ilimitado)
STRIPE_PRICE_ENTERPRISE_MONTHLY=price_your_enterprise_monthly_id
STRIPE_PRICE_ENTERPRISE_YEARLY=price_your_enterprise_yearly_id

# -----------------------------------------------------------------------------
# APPLICATION URLS
# -----------------------------------------------------------------------------
# Desenvolvimento
FRONTEND_URL=http://localhost:3000
API_URL=http://localhost:8000

# Produção (descomentar e ajustar)
# FRONTEND_URL=https://ifrs16-app.web.app
# API_URL=https://ifrs16-backend-1051753255664.us-central1.run.app

# -----------------------------------------------------------------------------
# EMAIL SMTP CONFIGURATION
# -----------------------------------------------------------------------------
# Configure seu provedor SMTP (Gmail, Zoho, SendGrid, etc)
SMTP_HOST=smtp.zoho.com
SMTP_PORT=587
SMTP_USER=your-email@domain.com
SMTP_PASSWORD=your-app-password-here
SMTP_FROM_EMAIL=noreply@yourdomain.com
SMTP_FROM_NAME=IFRS 16 - Sistema de Gestão
SMTP_USE_SSL=false
SMTP_USE_STARTTLS=true
SMTP_TIMEOUT_SECONDS=30

# -----------------------------------------------------------------------------
# ENVIRONMENT & DEBUG
# -----------------------------------------------------------------------------
# Valores: development | production
ENVIRONMENT=development
DEBUG=true

# -----------------------------------------------------------------------------
# CORS ORIGINS
# -----------------------------------------------------------------------------
# Lista separada por vírgulas das origens permitidas
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000,http://localhost:8000,file://

# Produção (adicionar):
# CORS_ORIGINS=https://ifrs16-app.web.app,https://ifrs16-app.firebaseapp.com,https://fxstudioai.com

# =============================================================================
# CHECKLIST DE SEGURANÇA PARA PRODUÇÃO
# =============================================================================
# [ ] JWT_SECRET_KEY alterado para valor forte e único
# [ ] ADMIN_TOKEN alterado para valor forte e único
# [ ] STRIPE_SECRET_KEY configurado com chave LIVE (sk_live_...)
# [ ] STRIPE_WEBHOOK_SECRET configurado com secret do webhook de produção
# [ ] DATABASE_URL aponta para banco de produção
# [ ] ENVIRONMENT=production
# [ ] DEBUG=false
# [ ] CORS_ORIGINS contém apenas domínios de produção
# [ ] SMTP configurado com credenciais válidas
# [ ] Arquivo .env NÃO está commitado no git
# =============================================================================
